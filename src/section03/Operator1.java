package section03;

public class Operator1 {
  public static void main(String[] args) {
    // 연산자 

    // 1. 단항
    // 1-1) 부호 : +1, -1
    // 1-2) 증감 : ++, --
    int n = 1;
    System.out.println(n++); // 이건 1이 나오고
    System.out.println(++n); // 이건 2가 나옴  >> ++이 먼저오냐 나중에 오냐에 따라 순서대로 진행 (같은선상이라 가정)
    // 1-3) 논리부정 (뒤집기) : !

    // 2. 이항 (변수 두가지로 연산)
    // ex. +, -, *, /, %, == 등등

    // 3. 삼항
  }
}
